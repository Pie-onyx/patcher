From 36fcf5da1abec6228252c11f323c3739e430c4e5 Mon Sep 17 00:00:00 2001
From: Adrian DC <radian.dc@gmail.com>
Date: Sun, 18 Aug 2019 01:31:20 +0200
Subject: [PATCH 44/44] kernel: disable CCACHE support for now

Change-Id: I39f968d257cccbd0929a9f13d96e349c6e14c04e
---
 core/tasks/kernel.mk | 26 +++++++++++++-------------
 1 file changed, 13 insertions(+), 13 deletions(-)

diff --git a/core/tasks/kernel.mk b/core/tasks/kernel.mk
index ea3f537..65944fb 100644
--- a/core/tasks/kernel.mk
+++ b/core/tasks/kernel.mk
@@ -226,19 +226,19 @@ KERNEL_TOOLCHAIN_PATH := $(KERNEL_TOOLCHAIN)/$(KERNEL_TOOLCHAIN_PREFIX)
 endif
 endif
 
-ifneq ($(USE_CCACHE),)
-    # Detect if the system already has ccache installed to use instead of the prebuilt
-    _ccache := $(shell which ccache)
-
-    ifeq ($(_ccache),)
-        _ccache := $(ANDROID_BUILD_TOP)/prebuilts/misc/$(HOST_PREBUILT_TAG)/ccache/ccache
-        # Check that the executable is here.
-        _ccache := $(strip $(wildcard $(_ccache)))
-    endif
-endif
-
-KERNEL_CROSS_COMPILE := CROSS_COMPILE="$(_ccache) $(KERNEL_TOOLCHAIN_PATH)"
-_ccache =
+# ifneq ($(USE_CCACHE),)
+#     # Detect if the system already has ccache installed to use instead of the prebuilt
+#     _ccache := $(shell which ccache)
+
+#     ifeq ($(_ccache),)
+#         _ccache := $(ANDROID_BUILD_TOP)/prebuilts/misc/$(HOST_PREBUILT_TAG)/ccache/ccache
+#         # Check that the executable is here.
+#         _ccache := $(strip $(wildcard $(_ccache)))
+#     endif
+# endif
+
+# KERNEL_CROSS_COMPILE := CROSS_COMPILE="$(_ccache) $(KERNEL_TOOLCHAIN_PATH)"
+# _ccache =
 
 define mv-modules
     mdpath=`find $(KERNEL_MODULES_OUT) -type f -name modules.order`;\
-- 
2.7.4

